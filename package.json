{
  "name": "topocopter",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A mobile-friendly Phaser topography quiz game.",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "generate:audio": "node scripts/generateAudioAssets.mjs"
  },
  "dependencies": {
    "phaser": "^3.70.0",
    "polygon-clipping": "^0.15.7"
  },
  "devDependencies": {
    "vite": "^5.4.10",
    "vitest": "^4.1.2"
  }
}
